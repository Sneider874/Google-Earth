# Dependencies
/node_modules
/npm-debug.log*
/yarn-error.log

# Production/Build files
/dist
/build
/environment files
# Es CRUCIAL ignorar el archivo .env ya que contiene secretos (JWT_SECRET, claves de BD, etc.)
.env

# Runtime data
/pids
/logs
*.pid
*.seed
*.log

# Editor/OS specific files (Archivos temporales del sistema o del editor)
.DS_Store
.vscode/
# .nodemon (si genera archivos temporales específicos, aunque generalmente no es necesario)
.nodemon